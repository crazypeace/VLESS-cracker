# 0.0 搭建一个可用的 Reality 服务端
```
curl -LO https://github.com/crazypeace/xray-vless-reality/raw/main/install.sh || wget -O ${_##*/} $_ && bash install.sh 4 8443
```

# 0.1 测试服务端伪装正常
```
curl -L --resolve 偷证书的域名:端口:VPS的公网IP  https://偷证书的域名:端口
```
例:
```
curl -L --resolve learn.microsoft.com:8443:167.160.187.180  https://learn.microsoft.com:8443
```

# 1.1 拉取xray镜像
```
apt install docker.io
docker pull ghcr.io/xtls/xray-core:latest
```

# 1.2 运行xray镜像
使用和现有 Reality 服务端同样的内核和配置
```
docker run -d \
  --name reality-server \
  --network bridge \
  -v /usr/local/bin/xray:/usr/local/bin/xray:ro \
  -v /usr/local/etc/xray/config.json:/usr/local/etc/xray/config.json:ro \
  ghcr.io/xtls/xray-core:latest 
```

# 1.3 检查docker使用的IP
```
docker ps -q | xargs docker inspect -f '{{.Name}} -> {{range.NetworkSettings.Networks}}{{.IPAddress}}{{end}}'
```

# 1.4 测试docker服务端伪装正常
```
curl -L --resolve 偷证书的域名:端口:Docker的IP  https://偷证书的域名:端口
```
例:
```
curl -L --resolve learn.microsoft.com:8443:172.17.0.2  https://learn.microsoft.com:8443
```

# 2.1 生成Reality客户端配置文件
```
{
  "log": {
    "loglevel": "warning"
  },
  "inbounds": [
    {
      "port": 1080,
      "listen": "127.0.0.1",
      "protocol": "socks",
      "settings": {
        "auth": "noauth",
        "udp": true
      },
      "sniffing": {
        "enabled": true,
        "destOverride": [
          "http",
          "tls"
        ]
      }
    }
  ],
  "outbounds": [
    {
      "protocol": "vless",
      "settings": {
        "vnext": [
          {
            "address": "172.17.0.2",	//***按实际情况修改
            "port": 8443,
            "users": [
              {
                "id": "2046c4dc-aed9-3e35-b047-e858a827181c",	//***按实际情况修改
                "flow": "xtls-rprx-vision",
                "encryption": "none"
              }
            ]
          }
        ]
      },
      "streamSettings": {
        "network": "tcp",
        "security": "reality",
        "realitySettings": {
          "serverName": "learn.microsoft.com",
          "publicKey": "WQvFYoH9cv3DDdEY5lPU1kj1DW7w19HgtApO7edwEmw",	//***按实际情况修改
          "shortId": "71790f91e17fa641",	//***按实际情况修改
          "spiderX": "/"
        }
      },
      "tcpSettings": {
        "header": {
          "type": "none"
        }
      }
    }
  ]
}
```

# 2.2 运行 Reality 客户端
```
usr/local/bin/xray run -config 客户端配置文件 &
```
例:
```
usr/local/bin/xray run -config /root/reality-client.json &
```

# 2.3 测试 Reality 客户端工作正常
```
curl -x socks://127.0.0.1:1080 google.com
```

# 3.1 克隆仓库
```
git clone https://github.com/Anonymous376c1d0cf28/VLESS-cracker
cd VLESS-cracker
```

# 3.2 安装编译依赖（Ubuntu/Debian）
```
sudo apt-get update
sudo apt-get install -y build-essential libpcap0.8-dev
```

# 3.3 编译 PoC
```
make
```

# 4.1 测试方法

a) docker中的Reality服务端是运行状态, 宿主机中的Reality客户端也是运行状态.

b) 运行POC程序
参数中指定抓包的网卡, 指定抓包的源终IP, 指定要测试的探针
```
./vless-cracker-v1 \
	-i docker0 \
	-f "tcp port 8443 and host 172.17.0.1 and host 172.17.0.2" \
	-P characteristic.txt  \
	-l info
```

c) 发起Reality数据包
```
curl -x socks://127.0.0.1:1080 google.com
```

d) 收集日志

e) 粘贴日志到分析工具中  
`vless-analyzer.html`

# 4.2 对于探针文件的说明
`characteristic.txt`  
是原作者的7个探针

`probes-1-10.txt`  
`probes-11-20.txt`  
`probes-21-30.txt`  
是 [issue 29](https://github.com/Anonymous376c1d0cf28/VLESS-cracker/issues/29) 的30个探针.  
其中, 第5, 18, 30号探针情况特殊, 需要单独测试. 在 `probes-*-*.txt` 文件中, 对应的位置填充了数据占位.  
也就是说, 当你使用 `probes-1-10.txt` `probes-11-20.txt` `probes-21-30.txt` 进行测试时, 并没有正确地实施 第5, 18, 30号探针

`probe-5-16385.txt`  
用原poc程序, 会因为探针体积大报错"probe file 'probe-5-16385.txt' line 1 is too long"   
需要使用 `probe-5-poc` 程序. 在程序中调整了 `#define MAX_PROBE_BYTES 32768`   
```
./probe-5-poc \
	-i docker0 \
	-f "tcp port 8443 and host 172.17.0.1 and host 172.17.0.2" \
	-P probe-5-16385.txt   \
	-l info
```

编译 `probe-5-poc` 的方法  
```
gcc -O2 -o probe-5-poc probe-5-mimo.c -lpcap -lpthread
```


`probe-18.txt`  
需要使用 `probe-18-poc` 程序. 在程序中调整了逻辑顺序, 将探针添加到 "client-hello" 的前面, 再合并一起发送.  
```
./probe-18-poc \
	-i docker0 \
	-f "tcp port 8443 and host 172.17.0.1 and host 172.17.0.2" \
	-P probe-18.txt  \
	-l info
```

编译 `probe-18-poc` 的方法  
```
gcc -O2 -o probe-18-poc probe-18-mimo.c -lpcap -lpthread
```

`probe-30.txt`  
单独测试即可  
```
./vless-cracker-v1 \
	-i docker0 \
	-f "tcp port 8443 and host 172.17.0.1 and host 172.17.0.2" \
	-P probe-30.txt     \
	-l info
```
